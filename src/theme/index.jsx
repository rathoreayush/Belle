import font from './font';
import {BORDER_RADIUS} from './boderRadius';
import {BUTTON_SIZES} from './button';
import {SPACING} from './spacing';
import responsive from './responsive';
import {COLORS} from './colors';
import fontsFamily from './fontsFamily';

export const theme = {
  font,
  BORDER_RADIUS,
  BUTTON_SIZES,
  SPACING,
  responsive,
  COLORS,
  fontsFamily,
};
